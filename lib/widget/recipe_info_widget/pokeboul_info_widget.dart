import 'package:flutter/material.dart';

class PokeboulInfoWidget extends StatelessWidget {
  const PokeboulInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
